Algoritmo "salario"
// Disciplina   : [Linguagem e L�gica de Programa��o]
// Professor   : Antonio Carlos Nicolodi 
// Descri��o   : Aqui voc� descreve o que o programa faz! (fun��o)
// Autor(a)    : Nome do(a) aluno(a)
// Data atual  : 03/01/2025
Var
// Se��o de Declara��es das vari�veis 
   pessoa1, pessoa2: caractere
 ValorHora, salario1, salario2, horas,soma: real

Inicio
// Se��o de Comandos, procedimento, fun��es, operadores, etc... 

      escreva ("Nome do funcion�rio(a): ")
      leia (pessoa1)
escreva ("Digite a quantidade de horas trabalhadas: ")
      leia (horas)
        ValorHora <- 90.00
      soma <- (salario1 + salario2)
      salario1 <- (ValorHora * horas)
      salario2 <- (ValorHora * horas)
      escreval (" O sal�rio de ", pessoa1, " � ", salario1, " reais.")
      
       escreva ("Nome do funcion�rio(a): ")
      leia (pessoa2)
escreva ("Digite a quantidade de horas trabalhadas: ")
      leia (horas)
        ValorHora <- 90.00
      soma <- (salario1 + salario2)
      salario1 <- (ValorHora * horas)
      salario2 <- (ValorHora * horas)
      escreval ("O sal�rio de ", pessoa2, " � de ", salario2, " reais.")
      escreval (" A soma dos sal�rios � de: ", soma)

Algoritmo "salario"
// Disciplina   : [Linguagem e L�gica de Programa��o]
// Professor   : Antonio Carlos Nicolodi 
// Descri��o   : Aqui voc� descreve o que o programa faz! (fun��o)
// Autor(a)    : Nome do(a) aluno(a)
// Data atual  : 03/01/2025
Var
// Se��o de Declara��es das vari�veis 
   pessoa1, pessoa2: caractere
 ValorHora, salario1, salario2, horas,soma: real

Inicio
// Se��o de Comandos, procedimento, fun��es, operadores, etc... 

      escreva ("Nome do funcion�rio(a): ")
      leia (pessoa1)
escreva ("Digite a quantidade de horas trabalhadas: ")
      leia (horas)
        ValorHora <- 90.00
      soma <- (salario1 + salario2)
      salario1 <- (ValorHora * horas)
      salario2 <- (ValorHora * horas)
      escreval (" O sal�rio de ", pessoa1, " � ", salario1, " reais.")
      
       escreva ("Nome do funcion�rio(a): ")
      leia (pessoa2)
escreva ("Digite a quantidade de horas trabalhadas: ")
      leia (horas)
        ValorHora <- 90.00
      soma <- (salario1 + salario2)
      salario1 <- (ValorHora * horas)
      salario2 <- (ValorHora * horas)
      escreval ("O sal�rio de ", pessoa2, " � de ", salario2, " reais.")
      escreval (" A soma dos sal�rios � de: ", soma)

Fimalgoritmo